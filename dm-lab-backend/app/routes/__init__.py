"""
Package initialiser for API blueprints.

The individual modules (auth, services, bookings, reports, finance) expose
``<blueprint_name>_bp`` variables which are registered by the application
factory.
"""
